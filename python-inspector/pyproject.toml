[project]
name = "x402-inspector"
version = "0.1.0"
description = "x402 v2 client written from scratch in Python (no web3/eth libs)"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0",
]
